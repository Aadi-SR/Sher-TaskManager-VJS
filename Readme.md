TASK LEDGER :  
its a demo task management application made with HTML, CSS and Vanilla JS.
![alt text](image.png)
Screenshot/demo (optional but valuable) — a quick visual beats paragraphs of description.
Features — bullet list of what it does (e.g. add/edit/delete tasks, filter by status/category, dark mode, localStorage persistence).
Getting started / how to run it — since this is a plain HTML/JS/CSS app with no build step, this could be as simple as "open index.html in a browser" or "serve the folder with any static server."
File structure — brief map of what's in the repo (index.html, app.js, styles.css, etc.) so a reader orients quickly.
Usage notes — anything non-obvious a user/dev should know (e.g. "data is stored in localStorage, not a server — clearing browser data clears your tasks").
Known limitations (optional, but honest) — e.g. "IDs use Date.now(), so rapid programmatic task creation could collide."
License (if relevant).
